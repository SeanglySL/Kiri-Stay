{
  "name": "kiri-stay-hotel-booking",
  "version": "1.0.0",
  "description": "Hotel room booking website — vanilla Node.js backend, no dependencies required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
